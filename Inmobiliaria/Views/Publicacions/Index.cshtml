@model IEnumerable<Inmobiliaria.Models.Publicacion>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.miIdInmuebe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fechaDePublicacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cantidadDeVisitas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.operacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.miIdInmuebe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fechaDePublicacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cantidadDeVisitas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.operacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
